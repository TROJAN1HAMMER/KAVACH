org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template.
#
# Left at the template default (false) deliberately: flipping it to `true`
# makes file_picker 11.x's Kotlin source compile (it skips the classic
# `org.jetbrains.kotlin.android` plugin on AGP 9+, expecting built-in Kotlin
# to handle it), but it then hard-fails flutter_plugin_android_lifecycle
# 2.0.35 — pulled in transitively by nearly every native plugin — which
# still applies the classic plugin unconditionally and AGP 9 refuses to let
# both mechanisms coexist. There is currently no gradle.properties setting
# that satisfies both; file_picker's ZIP-upload path is scoped out of this
# milestone instead (see docs/backend_gaps.md and start_scan_screen.dart).
android.builtInKotlin=false
