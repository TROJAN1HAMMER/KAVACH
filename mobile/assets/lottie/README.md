# Lottie assets

Drop `.json` Lottie animation files here (e.g. `loading.json`,
`empty_state.json`) and reference them from
`lib/widgets/common/loading_indicator.dart` or similar. None are bundled yet
— see the milestone report's "next milestone" list.
